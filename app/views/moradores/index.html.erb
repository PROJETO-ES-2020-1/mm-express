<p id="notice"><%= notice %></p>

<h1>moradores</h1>

<table>
  <thead>
    <tr>
      <th>Nome</th>
      <th>Email</th>
      <th>Telefone</th>
      <th>Cpf</th>
      <th>Numero residencia</th>
      <th>Bloco</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @moradores.each do |morador| %>
      <tr>
        <td><%= morador.nome %></td>
        <td><%= morador.email %></td>
        <td><%= morador.telefone %></td>
        <td><%= morador.cpf %></td>
        <td><%= morador.numero_residencia %></td>
        <td><%= morador.bloco %></td>
        <td><%= link_to 'Show', morador %></td>
        <td><%= link_to 'Edit', edit_morador_path(morador) %></td>
        <td><%= link_to 'Destroy', morador, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Morador', new_morador_path %>
